apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: tautulli
  namespace: media
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: tautulli
  endpoints:
    - port: http
      path: /metrics
