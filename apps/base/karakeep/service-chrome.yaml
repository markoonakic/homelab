apiVersion: v1
kind: Service
metadata:
  name: karakeep-chrome
  namespace: karakeep
spec:
  type: ClusterIP
  selector:
    app: karakeep
    component: chrome
  ports:
    - name: http
      port: 9222
      targetPort: http
      protocol: TCP
