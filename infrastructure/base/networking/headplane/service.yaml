apiVersion: v1
kind: Service
metadata:
  name: headplane
  namespace: headscale
spec:
  selector:
    app: headplane
  ports:
  - name: web
    port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP